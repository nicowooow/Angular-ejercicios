import { SortPriorityPipe } from './sort-priority-pipe';

describe('SortPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new SortPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
