import { ToDateFormatPipe } from './to-date-format-pipe';

describe('ToDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ToDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
